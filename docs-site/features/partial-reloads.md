# Partial reloads

`X-Inertia-Partial-Data` (`only`), `X-Inertia-Partial-Except` (`except`),
`X-Inertia-Partial-Component` and `X-Inertia-Reset` are honored with
dot-notation (`auth.user` selects nested keys). `Reset` clears merge
metadata selectively.
