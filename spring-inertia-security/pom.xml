<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.diovamny</groupId>
        <artifactId>inertia-parent</artifactId>
        <version>0.0.5</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>io.github.diovamny.spring.inertia</groupId>
    <artifactId>spring-inertia-security</artifactId>
    <packaging>jar</packaging>

    <name>Spring Inertia.js Security Integration</name>

    <properties>
        <!-- JaCoCo line floor, calibrated 2026-09-24 (measured 71.4%). -->
        <jacoco.line.minimum>0.65</jacoco.line.minimum>
        <!-- PIT floors, calibrated 2026-09-25 (measured 69% line, 42% kill). -->
        <pit.mutation.minimum>35</pit.mutation.minimum>
        <pit.coverage.minimum>60</pit.coverage.minimum>
        <!-- Spring Boot BOM uses JUnit 6: the PIT minion launcher must match
             the engine line (1.13.4 crashes discovery on 6.x). -->
        <junit-platform.version>6.1.3</junit-platform.version>
    </properties>
    <description>Inertia.js v3 protocol bridge for Spring Security (CSRF ownership, 409 challenges, 403 pages)</description>

    <dependencies>
        <dependency>
            <groupId>io.github.diovamny.spring.inertia</groupId>
            <artifactId>spring-inertia</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
