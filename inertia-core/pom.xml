<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.diovamny</groupId>
        <artifactId>inertia-parent</artifactId>
        <version>0.0.5</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>io.github.diovamny.inertia</groupId>
    <artifactId>inertia-core</artifactId>
    <packaging>jar</packaging>

    <name>Inertia.js Java Core Protocol</name>

    <properties>
        <!-- JaCoCo line floor, calibrated 2026-09-24 (measured 84.5%). -->
        <jacoco.line.minimum>0.80</jacoco.line.minimum>
        <!-- PIT floors, calibrated 2026-09-25 (measured 82% line, 70% kill). -->
        <pit.mutation.minimum>60</pit.mutation.minimum>
        <pit.coverage.minimum>75</pit.coverage.minimum>
    </properties>
    <description>Framework-agnostic Inertia.js v3 protocol model, SPI contracts,
        security utilities, server head builder and typed results shared by the
        Spring and Quarkus adapters</description>

    <dependencyManagement>
        <dependencies>
            <!-- The parent BOM pins junit-platform 6.x, which is incompatible
                 with the JUnit 5.13 engine used here; force the 1.13.4 line. -->
            <dependency>
                <groupId>org.junit.platform</groupId>
                <artifactId>junit-platform-commons</artifactId>
                <version>1.13.4</version>
            </dependency>
            <dependency>
                <groupId>org.junit.platform</groupId>
                <artifactId>junit-platform-engine</artifactId>
                <version>1.13.4</version>
            </dependency>
            <dependency>
                <groupId>org.junit.platform</groupId>
                <artifactId>junit-platform-launcher</artifactId>
                <version>1.13.4</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.13.4</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.13.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>5.13.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
