# Single source of truth for the project version (PLAN_MEJORA v4 §3.1 / H4).
# Bump this file together with the root pom; scripts/check-versions.mjs fails
# the build when any tracked source disagrees.
project.version=0.0.5
