server.port=8080
app.name=${appName}
inertia.root-template=index.html
# SSR sidecar (Fase D): npm run build:ssr && node src/main/webui/ssr-server.mjs,
# then uncomment the next two lines to render through it (CSR fallback stays).
#inertia.ssr-enabled=true
#inertia.ssr-url=http://localhost:13714/render
